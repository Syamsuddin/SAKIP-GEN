# SAKIP-Gen — dependensi
# Fase 0 memakai: aiogram, fastapi, uvicorn, pydantic, pydantic-settings.
# Paket lain disiapkan untuk fase berikutnya (data, evaluasi, usulan, penjadwalan).
aiogram>=3.7,<4
fastapi>=0.110
uvicorn[standard]>=0.29
gunicorn>=21
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27

# Fase 1+
SQLAlchemy>=2.0
asyncmy>=0.2.9
jinja2>=3.1
itsdangerous>=2.1
APScheduler>=3.10
anthropic>=0.25

# Fase 4 — migrasi tabel milik agen (ai_*)
alembic>=1.13

# Fase 5 — rate-limit terdistribusi (opsional; hanya dipakai bila REDIS_URL diset)
redis>=5
